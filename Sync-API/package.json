{
  "name": "sync-api",
  "version": "1.0.0",
  "description": "Roblox-protected script API for SyncHook",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "dev": "vercel dev",
    "build": "vercel build",
    "deploy": "vercel deploy --prod",
    "start": "vercel dev"
  },
  "dependencies": {
    "@upstash/redis": "^1.34.0"
  },
  "devDependencies": {
    "vercel": "^39.0.0"
  }
}
